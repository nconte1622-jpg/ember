# App reads these public vars to find your deployed proxy (safe to expose — they're just URLs).
EXPO_PUBLIC_EMBER_API_URL=https://your-proxy.vercel.app/api/draft
EXPO_PUBLIC_EMBER_CAPTURE_URL=https://your-proxy.vercel.app/api/capture

# This lives